# Runtime deps the notebook actually imports, pinned to match the global
# project. torch-brain pulls numpy/pandas/h5py/tqdm/ray/rich transitively.
torch-brain==0.2.0a0
torch==2.10.0
scikit-learn==1.7.2
matplotlib
tqdm

# notebook <-> .ipynb conversion and the Jupyter kernel Quarto renders through
jupytext>=1.16
ipykernel
nbclient
jupyter-client
nbformat
