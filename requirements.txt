# Runtime deps the notebook actually imports, pinned to match the global
# project. torch-brain pulls numpy/pandas/h5py/tqdm/ray/rich transitively.
# torch, scikit-learn, and matplotlib are intentionally left unpinned in
# requirements-dev.txt instead: they're preinstalled on Colab, so no version
# here is needed to run the notebook there.
torch_brain @ git+https://github.com/neuro-galaxy/torch_brain.git@deb39f026da33a93f9a95884eda82c1aa60dcd1a
tqdm
huggingface_hub
bokeh
